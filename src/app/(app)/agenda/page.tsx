import { AgendaClient } from "@/components/agenda/agenda-client";

export const metadata = {
  title: "Agenda & RDV — HawaeMD",
};

export default function AgendaPage() {
  return <AgendaClient />;
}
