import { RappelsClient } from "@/components/rappels/rappels-client";

export const metadata = {
  title: "Rappels — HawaeMD",
};

export default function RappelsPage() {
  return <RappelsClient />;
}
