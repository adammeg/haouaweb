import { ScoresClient } from "@/components/scores/scores-client";

export const metadata = {
  title: "Scores cliniques — HawaeMD",
};

export default function ScoresPage() {
  return <ScoresClient />;
}
